function launch_requirements_checker()
%LAUNCH_REQUIREMENTS_CHECKER Friendly entry point that opens the UI.
%   Simply run LAUNCH_REQUIREMENTS_CHECKER from MATLAB to open the
%   requirements checker dashboard with emoji statuses and scan buttons.

    requirements_checker_ui();
end
